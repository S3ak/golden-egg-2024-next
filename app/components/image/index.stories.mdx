import { Canvas, Meta } from "@storybook/blocks";

import * as Image from "./index.stories";

<Meta of={Image} />

# Image

DEfault Image component. It wraps the native `img` element.

<Canvas of={Image} />
